<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dm.cn.system.mapper.SysLogininforMapper">

	<resultMap type="com.dm.cn.system.entity.server.SysLogininfor" id="SysLogininforResult">
		<id     property="infoId"        column="info_id"           />
		<result property="userName"      column="user_name"         />
		<result property="status"        column="status"            />
		<result property="ipaddr"        column="ipaddr"            />
		<result property="msg"           column="msg"               />
		<result property="accessTime"    column="access_time"       />
	</resultMap>

	<insert id="insertLogininfor" parameterType="com.dm.cn.system.entity.server.SysLogininfor">
		insert into sys_logininfor (user_name, status, ipaddr, msg, access_time)
		values (#{userName}, #{status}, #{ipaddr}, #{msg}, now())
	</insert>
	<!--h2-->
	<select id="selectLogininforList" parameterType="com.dm.cn.system.entity.server.SysLogininfor" resultMap="SysLogininforResult">
		select info_id, user_name, ipaddr, status, msg, access_time from sys_logininfor
		<where>
			<if test="status != null and status != ''">
				AND status = #{status}
			</if>
			<if test="userName != null and userName != ''">
				AND (user_name like concat('%', #{userName}, '%') or ipaddr like concat('%', #{userName}, '%'))
			</if>
			<if test="params.beginTime != null and params.beginTime != '' and params.endTime != null and params.endTime != ''" >
				AND access_time BETWEEN #{params.beginTime} AND #{params.endTime}
			</if>
		</where>
		order by info_id desc
	</select>

	<delete id="deleteLogininforByIds" parameterType="Long">
 		delete from sys_logininfor where info_id in
 		<foreach collection="array" item="infoId" open="(" separator="," close=")">
 			#{infoId}
        </foreach>
 	</delete>

    <update id="cleanLogininfor">
        truncate table sys_logininfor
    </update>


	<select id="getLastLoginUser" resultType="com.dm.cn.system.entity.server.SysLogininfor">
		select * from SYS_LOGININFOR where USER_NAME = #{username} order by ACCESS_TIME desc limit 1
	</select>

</mapper>
